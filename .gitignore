# Banco de dados local — NUNCA versionar nem sincronizar (corrompe entre máquinas)
backend/pgdata/
pgdata/

# Dependências: VERSIONADAS de propósito (puro JS + WASM, portáveis) para o
# Linux/qualquer máquina "clonar e rodar" sem npm install nem internet.

# Backups exportados (dados) — mantém só o LEIA-ME da pasta
_backups/*.json

# Lixo do sistema operacional / Google Drive
.DS_Store
._*
.tmp.drive*
* 2.*
